name: 'Hello World'

on:
    push:
        branches: [ "main" ]
    pull_request:
        branches: [ "main" ]

jobs:
    hello:
        runs-on: ubuntu:latest
        steps:
            - name: hello-world
              run: echo :"Hello World"
          